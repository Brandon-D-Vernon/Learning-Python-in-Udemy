#type conversion

# 100 is a string (str) first
# Then we turn 100 into a interger (int)
# then we check what type 100 is. last type change was integer (int) so 100 is integer (int).

print(type(int(str(100)))) 

#another example 
# a = str(100)
# b = int(a)
# c = type(b)
# print(c)