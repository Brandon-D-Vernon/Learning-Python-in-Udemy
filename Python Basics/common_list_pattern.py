#common list patterns

basket = ['a', 'x', 'b', 'c', 'd', 'e', 'd']
basket.sort() # Sort letters in abc order [a,b,c,d,d,x]
basket.reverse() # Reverse letters order [x,d,d,c,b,a]
# print(basket[::-1]) # Slicing letters back to abc order [a,b,c,d,d,x]
# print(basket)

# print(list(range(101))) # Range print all numbers counting to 100

# sentence = ' '

new_sentence = ' '.join(['hi', 'my', 'name', 'is', 'JOJO'])

print(new_sentence)