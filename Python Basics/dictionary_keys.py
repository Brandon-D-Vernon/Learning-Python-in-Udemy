#dictionary keys
# A dictionary key needs to be inmuttable. a key cannot change.
# Keys has to be unigue, you don't want keys to have the same name.

dictionary = {
  123: [1,2,3],
  True: 'hello',
  [100] : True
}


print(dictionary[100])