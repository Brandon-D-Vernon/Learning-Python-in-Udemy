#list method 3

# .sort - It sort out the list in abc order
# sorted creates a new copy of the variable and sorts it.
basket = ['a', 'x', 'b', 'c', 'd', 'e', 'd']
basket.sort()
# print(sorted(basket))
# basket.copy #make another copy of the list
basket.reverse() #reverse to letters backward
print(basket)